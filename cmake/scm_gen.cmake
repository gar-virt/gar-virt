set(INPUT_FILE "${CMAKE_ARGV3}")
set(OUTPUT_FILE "${CMAKE_ARGV4}")
set(SCM_VERSION_FILE "${CMAKE_ARGV5}")
set(VARIABLE_NAME "${CMAKE_ARGV6}")
file(STRINGS "${SCM_VERSION_FILE}" ${VARIABLE_NAME} LIMIT_COUNT 1)
configure_file("${INPUT_FILE}" "${OUTPUT_FILE}" @ONLY NEWLINE_STYLE LF)
