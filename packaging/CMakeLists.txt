set(CPACK_PACKAGE_NAME "gar-virt")
set(CPACK_PACKAGE_VERSION ${PROJECT_VERSION})
set(CPACK_PACKAGE_VENDOR "gar-virt")
set(CPACK_PACKAGE_CONTACT "gar-virt")
set(CPACK_COMPONENTS_GROUPING ALL_COMPONENTS_IN_ONE)

set(CPACK_ARCHIVE_COMPONENT_INSTALL ON)

set(CPACK_DEB_COMPONENT_INSTALL ON)
set(CPACK_DEBIAN_FILE_NAME DEB-DEFAULT)
set(CPACK_DEBIAN_PACKAGE_SECTION "admin")
set(CPACK_DEBIAN_PACKAGE_DEPENDS "ca-certificates, libc6 (>= 2.27), libvirt0 (>=4)")
set(CPACK_DEBIAN_PACKAGE_RECOMMENDS "libssh-4, openssh-client")

include(CPack)
